public interface CarsBuilder {
    CarsBuilder setColor(String color);
    CarsBuilder setWheels(String wheels);
    CarsBuilder setTransmission(String transmission);
    CarsBuilder setEngine(String engine);

}